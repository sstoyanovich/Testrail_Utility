const platforms = {
  Web: 6,
};

const priorities = {
  p0: 4,
  p1: 3,
  p2: 2,
};

const testTypes = {
  regression: 9,
};

const regions = {
  us: 12,
  core: 14,
};

module.exports = { platforms, priorities, testTypes, regions };
